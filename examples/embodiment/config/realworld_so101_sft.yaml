# SO101 arm offline SFT (behavior cloning) training config.
# Trains a policy from collected demonstration data.
#
# Usage:
#   python examples/embodiment/train_embodied_agent.py \
#       --config-name realworld_so101_sft

defaults:
  - env/realworld_so101@env.train
  - env/realworld_so101@env.eval
  - model/cnn_policy@actor.model
  - training_backend/fsdp@actor.fsdp_config
  - weight_syncer/patch_syncer@weight_syncer
  - override hydra/job_logging: stdout

hydra:
  run:
    dir: .
  output_subdir: null
  searchpath:
    - file://${oc.env:EMBODIED_PATH}/config/

cluster:
  num_nodes: 1
  component_placement:
    actor: 0-0
    env: 0-0
    rollout: 0-0

runner:
  task_type: embodied
  logger:
    log_path: "../results"
    project_name: rlinf
    experiment_name: "so101-sft"
    logger_backends: ["tensorboard"]

  max_epochs: 50
  max_steps: -1

  only_eval: False
  val_check_interval: 5
  save_interval: 5

  resume_dir: null
  ckpt_path: null

algorithm:
  update_epoch: 1
  group_size: 1

  rollout_epoch: 1
  eval_rollout_epoch: 1

  adv_type: embodied_sft
  loss_type: embodied_sft
  loss_agg_func: "token-mean"

  # Demo buffer for SFT
  demo_buffer:
    dataset_path: /path/to/collected_data
    dataset_type: "pickle"

env:
  group_name: "EnvGroup"

  train:
    total_num_envs: 1
    override_cfg:
      is_dummy: True

  eval:
    total_num_envs: 1
    override_cfg:
      is_dummy: True

rollout:
  group_name: "RolloutGroup"
  backend: "huggingface"
  enable_offload: False
  pipeline_stage_num: 1

  collect_transitions: False
  collect_prev_infos: False

  model:
    model_path: "/path/to/model"
    precision: ${actor.model.precision}

actor:
  group_name: "ActorGroup"
  training_backend: "fsdp"

  micro_batch_size: 256
  global_batch_size: 256
  seed: 1234
  enable_offload: False

  model:
    model_path: "/path/to/model"
    state_dim: 7   # 6 joint positions + 1 gripper
    action_dim: 7  # 6 joint targets + 1 gripper target

  optim:
    clip_grad: 10.0
    lr: 1e-4
    lr_scheduler: torch_cosine

  fsdp_config:
    strategy: "fsdp"
    sharding_strategy: "no_shard"
    mixed_precision:
      param_dtype: ${actor.model.precision}
      reduce_dtype: ${actor.model.precision}
      buffer_dtype: ${actor.model.precision}

reward:
  use_reward_model: False

critic:
  use_critic_model: False
